% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility.R
\name{gqr_file_provenance}
\alias{gqr_file_provenance}
\title{Record provenance for an input file}
\usage{
gqr_file_provenance(
  path,
  original_name = basename(path),
  role = "dataset",
  data = NULL
)
}
\arguments{
\item{path}{Path to the file currently available to R. In a Shiny upload
this is normally the temporary upload path.}

\item{original_name}{Original file name to report. For Shiny uploads this
should be the browser-supplied file name rather than the temporary path.}

\item{role}{Short description of the file's role, such as \code{"dataset"} or
\code{"groups"}.}

\item{data}{Optional data frame read from the file. When supplied, row and
column counts are added to the provenance record.}
}
\value{
A named list of class \code{gqr_file_provenance}.
}
\description{
Creates a compact provenance record for a file used in a GQR analysis.
The record stores the original user-facing file name, file size, an MD5
checksum, and (when a data frame is supplied) its dimensions. This is used
by the Shiny application's reproducible-script tab, but can also be used in
scripted workflows.
}
\examples{
path <- system.file("extdata", "dummy_data.RDA", package = "GQR")
if (nzchar(path)) {
  dat <- gqr_read(path)
  gqr_file_provenance(path, original_name = "dummy_data.RDA", data = dat)
}

}
