% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dummies.R
\name{gqr_estimate_design}
\alias{gqr_estimate_design}
\title{Estimate the size of a Generalised Q design}
\usage{
gqr_estimate_design(
  variables,
  mode = c("all", "group_one_per", "random"),
  groups = NULL,
  n_patterns = 1000L,
  n_respondents = NULL,
  allow_ungrouped = FALSE
)
}
\arguments{
\item{variables}{Character vector of simple-statement names, or a single
positive number giving the statement count.}

\item{mode}{One of \code{"all"}, \code{"group_one_per"}, or \code{"random"}.}

\item{groups}{Group specification required for grouped mode. Supply either a
named list of variables or a data frame with columns \code{group} and \code{variable}.}

\item{n_patterns}{Number of patterns requested in random mode.}

\item{n_respondents}{Optional respondent count used to estimate W memory.}

\item{allow_ungrouped}{Whether grouped mode may leave variables permanently
inactive.}
}
\value{
A one-row data frame containing the mode, variable count, pattern
count, estimated dummy-matrix memory in MiB, and estimated W-matrix memory
in MiB.
}
\description{
Calculates the number of synthetic statement patterns and approximate
memory requirements before allocating the design matrix \code{D} or evaluation
matrix \code{W}.
}
\details{
Full binary designs contain \code{2^m} patterns for \code{m} statements. Grouped
designs contain the product of the group sizes. Random designs contain the
requested fixed number of rows. Estimates assume four bytes per integer
dummy value and eight bytes per numeric W value; temporary copies and PCA
objects require additional memory.
}
\examples{
gqr_estimate_design(
  variables = paste0("Q", 1:9),
  mode = "all",
  n_respondents = 10
)

groups <- list(
  Question_1 = c("Q1", "Q2", "Q3"),
  Question_2 = c("Q4", "Q5", "Q6"),
  Question_3 = c("Q7", "Q8", "Q9")
)
gqr_estimate_design(paste0("Q", 1:9), "group_one_per", groups)

}
\references{
Dentinho, T. P., Kourtit, K., & Nijkamp, P. (2023). Generalized Q analysis
as a new tool in social science research: A pedagogical introduction.
\emph{Eastern Journal of European Studies}, \strong{14}(2), 5--21.
\doi{10.47743/ejes-2023-0201}
}
