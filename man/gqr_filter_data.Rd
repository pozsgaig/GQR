% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-preparation.R
\name{gqr_filter_data}
\alias{gqr_filter_data}
\title{Filter respondents before Generalised Q analysis}
\usage{
gqr_filter_data(data, id_col = NULL, ids = NULL, filters = NULL)
}
\arguments{
\item{data}{A respondent-level data frame.}

\item{id_col}{Optional name of the respondent identifier column.}

\item{ids}{Optional vector of identifiers to retain.}

\item{filters}{Optional named list. For a numeric column, a numeric vector of
length two defines an inclusive range. Other vectors define allowed values.
A function may also be supplied and must return one logical value per row.}
}
\value{
A data frame containing the retained respondents in their original
order.
}
\description{
Retains a subset of respondents by identifier and/or named column rules
before the synthetic statement matrix is constructed.
}
\details{
Identifier and filter conditions are combined with logical AND. Missing
filter results are treated as \code{FALSE}. An error is returned when no
respondents remain, preventing a later failure during W-matrix construction.
}
\examples{
dat <- gqr_example_data("dummy_data")

gqr_filter_data(
  dat,
  id_col = "Respondent",
  ids = c("R1", "R2", "R3")
)

gqr_filter_data(
  dat,
  filters = list(Q1 = range(dat$Q1), Q2 = function(x) x >= median(x))
)

}
\references{
Dentinho, T. P., Kourtit, K., & Nijkamp, P. (2023). Generalized Q analysis
as a new tool in social science research: A pedagogical introduction.
\emph{Eastern Journal of European Studies}, \strong{14}(2), 5--21.
\doi{10.47743/ejes-2023-0201}
}
