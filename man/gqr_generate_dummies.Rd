% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dummies.R
\name{gqr_generate_dummies}
\alias{gqr_generate_dummies}
\title{Generate the synthetic-statement design matrix}
\usage{
gqr_generate_dummies(
  variables,
  mode = c("all", "group_one_per", "random"),
  groups = NULL,
  n_patterns = 1000L,
  prob = 0.5,
  seed = NULL,
  include_empty = TRUE,
  allow_ungrouped = FALSE,
  max_patterns = 1000000L
)
}
\arguments{
\item{variables}{Character vector of unique simple-statement names.}

\item{mode}{One of \code{"all"}, \code{"group_one_per"}, or \code{"random"}.}

\item{groups}{Named list or data frame with columns \code{group} and \code{variable}
for grouped mode.}

\item{n_patterns}{Number of rows sampled in random mode.}

\item{prob}{Probability that each statement is active in random mode.}

\item{seed}{Optional seed used only for random mode.}

\item{include_empty}{Whether the all-zero combination is retained.}

\item{allow_ungrouped}{Whether grouped mode may leave variables permanently
inactive.}

\item{max_patterns}{Maximum permitted number of rows, checked before
allocation.}
}
\value{
An integer zero/one matrix with named columns and synthetic statement
row names \code{S1}, \code{S2}, and so forth.
}
\description{
Creates the binary matrix \code{D} used by Generalised Q analysis. Rows represent
synthetic combined statements and columns represent the original simple
statements.
}
\details{
In full mode, the rows follow binary counting. In grouped mode, exactly one
variable is active in every group, implementing the structured recombination
described by Dentinho et al. (2023). In random mode, entries are independent
Bernoulli draws; results should be checked across seeds or larger samples.

Use \code{\link[=gqr_estimate_design]{gqr_estimate_design()}} before full or large grouped designs.
}
\examples{
gqr_generate_dummies(c("A", "B", "C"), mode = "all")

groups <- data.frame(
  group = c("Question 1", "Question 1", "Question 2", "Question 2"),
  variable = c("A", "B", "C", "D")
)
gqr_generate_dummies(c("A", "B", "C", "D"), "group_one_per", groups)

gqr_generate_dummies(
  paste0("Q", 1:20),
  mode = "random",
  n_patterns = 1000,
  seed = 42
)

}
\references{
Dentinho, T. P., Kourtit, K., & Nijkamp, P. (2023). Generalized Q analysis
as a new tool in social science research: A pedagogical introduction.
\emph{Eastern Journal of European Studies}, \strong{14}(2), 5--21.
\doi{10.47743/ejes-2023-0201}
}
