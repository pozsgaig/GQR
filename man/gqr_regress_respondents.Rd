% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressions.R
\name{gqr_regress_respondents}
\alias{gqr_regress_respondents}
\title{Regress respondent loadings on covariates}
\usage{
gqr_regress_respondents(pca, metadata, id_col, covariates, components = NULL)
}
\arguments{
\item{pca}{A result from \code{\link[=gqr_pca]{gqr_pca()}}.}

\item{metadata}{Respondent-level data containing identifiers and covariates.}

\item{id_col}{Name of the column matching respondent IDs in the PCA loadings.}

\item{covariates}{Character vector naming predictor columns.}

\item{components}{Optional component names; all retained components are used
by default.}
}
\value{
An object of class \code{gqr_respondent_regression} containing the aligned
modelling data, fitted models, combined coefficient table, and usable
covariate names.
}
\description{
Fits one model per selected component to relate respondent loadings to
demographic, geographic, behavioural, or other respondent-level variables.
}
\details{
Respondent identifiers must be unique and complete. Covariates without
variation are excluded, and at least one usable covariate must remain.
Factors are represented through the standard treatment contrasts used by
\code{\link[stats:lm]{stats::lm()}}. These regressions support interpretation but do not establish
causation.
}
\examples{
dat <- gqr_example_data("dummy_data")
fit <- gqr_analysis(
  dat,
  analysis_cols = paste0("Q", 1:9),
  id_col = "Respondent",
  n_components = 2,
  statement_regression = FALSE,
  respondent_regression = FALSE
)

metadata <- transform(
  dat,
  Segment = rep(c("A", "B"), length.out = nrow(dat)),
  Sequence = seq_len(nrow(dat))
)

respondent_models <- gqr_regress_respondents(
  pca = fit$pca,
  metadata = metadata,
  id_col = "Respondent",
  covariates = c("Segment", "Sequence")
)
head(respondent_models$coefficients)

}
\references{
Dentinho, T. P., Kourtit, K., & Nijkamp, P. (2023). Generalized Q analysis
as a new tool in social science research: A pedagogical introduction.
\emph{Eastern Journal of European Studies}, \strong{14}(2), 5--21.
\doi{10.47743/ejes-2023-0201}
}
