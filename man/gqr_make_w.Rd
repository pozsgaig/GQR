% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/w-matrix.R
\name{gqr_make_w}
\alias{gqr_make_w}
\title{Construct the synthetic evaluation matrix}
\usage{
gqr_make_w(
  data,
  analysis_cols = NULL,
  D,
  id_col = NULL,
  na_action = c("error", "mean", "zero")
)
}
\arguments{
\item{data}{A respondent-level data frame or a \code{gqr_prepared_data} object.}

\item{analysis_cols}{Analysis-column names when \code{data} is a plain data frame.}

\item{D}{A named binary design matrix produced by
\code{\link[=gqr_generate_dummies]{gqr_generate_dummies()}}.}

\item{id_col}{Optional respondent identifier column when \code{data} is plain.}

\item{na_action}{Handling of non-finite analysis values: \code{"error"},
\code{"mean"}, or \code{"zero"}.}
}
\value{
A numeric matrix with synthetic combined statements in rows and
respondents in columns.
}
\description{
Combines the binary synthetic-statement design with respondent evaluations to
calculate the Generalised Q matrix \code{W}.
}
\details{
Let \code{V} be the respondent-by-statement matrix and \code{D} the
combination-by-statement design. The function calculates

\deqn{W = D V^\mathsf{T}.}

Therefore, \code{W[i, j]} is the sum of respondent \code{j}'s values for the simple
statements active in combination \code{i}. This encodes the additive-separability
assumption central to Generalised Q analysis.
}
\examples{
dat <- gqr_example_data("dummy_data")
prepared <- gqr_prepare_data(
  dat,
  analysis_cols = paste0("Q", 1:9),
  id_col = "Respondent"
)
D <- gqr_generate_dummies(prepared$analysis_cols, mode = "all")
W <- gqr_make_w(prepared, D = D)
dim(W)

}
\references{
Dentinho, T. P., Kourtit, K., & Nijkamp, P. (2023). Generalized Q analysis
as a new tool in social science research: A pedagogical introduction.
\emph{Eastern Journal of European Studies}, \strong{14}(2), 5--21.
\doi{10.47743/ejes-2023-0201}
}
