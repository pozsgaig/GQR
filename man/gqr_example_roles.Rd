% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-data.R
\name{gqr_example_roles}
\alias{gqr_example_roles}
\title{Recommended column roles for bundled GQR examples}
\usage{
gqr_example_roles(dataset = c("dummy_data", "gardening"))
}
\arguments{
\item{dataset}{Either \code{"dummy_data"} or \code{"gardening"}.}
}
\value{
A list with \code{analysis_cols}, \code{covariate_cols}, and \code{id_col}.
}
\description{
Returns the column roles used by the GQR Shiny application for a bundled
example dataset. Using this helper in scripts keeps the graphical and
programmatic interfaces aligned.
}
\details{
For \code{dummy_data}, the nine \code{Q} columns are analysis variables and
\code{Numeric_covariate} plus \code{Factor_covariate} are the recommended covariates.
For \code{gardening}, numeric questionnaire variables are used as analysis
variables, while the non-numeric respondent descriptors other than \code{ID} are
returned as covariates. These are starting defaults only; users can override
them in either interface.
}
\examples{
roles <- gqr_example_roles("dummy_data")
roles$analysis_cols
roles$covariate_cols

dat <- gqr_example_data("dummy_data")
fit <- gqr_analysis(
  dat,
  analysis_cols = roles$analysis_cols,
  id_col = roles$id_col,
  covariate_cols = roles$covariate_cols,
  n_components = 3
)

}
