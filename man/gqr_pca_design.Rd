% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca-design.R
\name{gqr_pca_design}
\alias{gqr_pca_design}
\title{Fast exact PCA without materialising the complete W matrix}
\usage{
gqr_pca_design(
  data,
  D,
  analysis_cols = NULL,
  id_col = NULL,
  n_components = NULL,
  rotation = c("none", "varimax"),
  center = TRUE,
  scale = TRUE,
  na_action = c("error", "mean", "zero"),
  remove_constant = TRUE,
  tolerance = 1e-08,
  score_chunk_size = 10000L,
  progress = NULL,
  cancel = NULL
)
}
\arguments{
\item{data}{A respondent-level data frame or a \code{gqr_prepared_data} object.}

\item{D}{A named binary design matrix produced by \code{\link[=gqr_generate_dummies]{gqr_generate_dummies()}}.}

\item{analysis_cols}{Analysis-column names when \code{data} is a plain data frame.}

\item{id_col}{Optional respondent identifier column when \code{data} is plain.}

\item{n_components}{Number of components to retain. If \code{NULL}, all positive
components are retained.}

\item{rotation}{One of \code{"none"} or \code{"varimax"}.}

\item{center, scale}{Logical centring and scaling settings matching
\code{\link[stats:prcomp]{stats::prcomp()}}.}

\item{na_action}{Handling of non-finite analysis values: \code{"error"},
\code{"mean"}, or \code{"zero"}.}

\item{remove_constant}{Whether respondent columns with zero variance are
removed before PCA.}

\item{tolerance}{Numerical tolerance used for ranks and zero variance.}

\item{score_chunk_size}{Number of synthetic statements processed per block
when component scores are generated.}

\item{progress}{Optional callback receiving \code{value} (0--1) and \code{message}.}

\item{cancel}{Optional zero-argument callback. Returning \code{TRUE} stops the
calculation at the next safe checkpoint.}
}
\value{
An object of class \code{gqr_pca_result}, with the same principal fields
as \code{\link[=gqr_pca]{gqr_pca()}}. \code{method} is reported as \code{"design"}.
}
\description{
Computes the ordinary (\code{prcomp}-style) Generalised Q PCA directly from the
original respondent-by-statement data and the dummy design matrix \code{D}.
Because \verb{W = D V^T} has rank no greater than the number of original
statements, the decomposition can be carried out in this much smaller
statement space. This can be substantially faster and use much less memory
when \code{W} has many rows or respondents.
}
\details{
For ordinary PCA, materialising \code{W} is unnecessary. After the requested
centring and scaling, the respondent cross-product can be written using the
much smaller \verb{m x m} cross-product of \code{D}, where \code{m} is the number of simple
statements. GQR factorises that matrix and performs an SVD in a space whose
rank is at most \code{m}. Synthetic-statement scores are then reconstructed in
blocks. The result is algebraically equivalent to ordinary PCA on the full W
matrix, apart from the arbitrary signs of PCA axes and floating-point error.

This optimisation applies to ordinary PCA. The SPSS-style smoothed
correlation workflow implemented by \code{\link[=gqr_pca]{gqr_pca()}} still requires the full W
matrix.
}
\examples{
dat <- gqr_example_data("dummy_data")
prepared <- gqr_prepare_data(dat, paste0("Q", 1:9), id_col = "Respondent")
D <- gqr_generate_dummies(prepared$analysis_cols)

fast <- gqr_pca_design(
  prepared,
  D,
  n_components = 3,
  rotation = "varimax"
)
fast$variance_explained

}
\references{
Dentinho, T. P., Kourtit, K., & Nijkamp, P. (2023). Generalized Q analysis
as a new tool in social science research: A pedagogical introduction.
\emph{Eastern Journal of European Studies}, \strong{14}(2), 5--21.
\doi{10.47743/ejes-2023-0201}
}
